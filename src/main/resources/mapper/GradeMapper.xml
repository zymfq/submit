<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhy.submit.teacher.mapper.GradeMapper">
  <resultMap id="BaseResultMap" type="com.zhy.submit.teacher.entity.Grade">
    <id column="grade_id" jdbcType="INTEGER" property="gradeId" />
    <result column="grade_name" jdbcType="VARCHAR" property="gradeName" />
  </resultMap>
  <sql id="Base_Column_List">
    grade_id, grade_name
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from grade
    where grade_id = #{gradeId,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from grade
    where grade_id = #{gradeId,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.zhy.submit.teacher.entity.Grade">
    insert into grade (grade_id, grade_name)
    values (#{gradeId,jdbcType=INTEGER}, #{gradeName,jdbcType=VARCHAR})
  </insert>

  <update id="updateByPrimaryKeySelective" parameterType="com.zhy.submit.teacher.entity.Grade">
    update grade
    <set>
      <if test="gradeName != null">
        grade_name = #{gradeName,jdbcType=VARCHAR},
      </if>
    </set>
    where grade_id = #{gradeId,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.zhy.submit.teacher.entity.Grade">
    update grade
    set grade_name = #{gradeName,jdbcType=VARCHAR}
    where grade_id = #{gradeId,jdbcType=INTEGER}
  </update>
</mapper>